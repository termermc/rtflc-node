// Measure function call overhead in a hot loop.

func square(x) {
  return mul(x, x)
}

def n = 200000

def i = 0
def sum = 0

while [i < n] {
  sum = add(sum, square(i))
  i = add(i, 1)
}

println(sum)
